{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "peer-loan",
  "scripts": {
    "build": "cd src/peerLoan_frontend && npm run build",
    "prebuild": "cd src/peerLoan_frontend && npm run prebuild",
    "pretest": "cd src/peerLoan_frontend && npm run prebuild",
    "start": "cd src/peerLoan_frontend && npm start",
    "test": "cd src/peerLoan_frontend && npm test"
  },
  "type": "module"
}